# Domain Docs

Before exploring, read root `CONTEXT.md` and relevant `docs/adr/` entries when they exist. Proceed silently when they do not.

This is a single-context repo:

```
/
├── CONTEXT.md
└── docs/adr/
```

Use terms defined in `CONTEXT.md`; surface conflicts with existing ADRs rather than silently overriding them.
